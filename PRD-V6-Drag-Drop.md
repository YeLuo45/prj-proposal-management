# PRD: V6 - 看板拖拽排序

## 目标
实现看板/泳道视图的拖拽排序功能，支持拖拽卡片到不同状态/位置。

## 功能范围

### 核心功能
- 卡片可拖拽
- 拖拽到同泳道不同位置排序
- 拖拽到不同泳道（改变状态）
- 拖放时显示视觉占位符
- 拖放后自动保存到localStorage

### 技术方案
- 使用 react-beautiful-dnd 或原生拖拽 API
- 数据结构不变，复用现有 proposals 数组
- 状态变更通过 updateProject Proposals 同步

## 交付目标
- 拖拽流畅无卡顿
- 状态变更正确同步
- 构建通过
